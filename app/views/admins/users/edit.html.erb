<div class="container">
<h1><%= @user.nickname %></h1>

<%= form_for @user, url: admins_user_path, method: :patch do |f| %>

	<%= f.radio_button :is_active, "Available" %>有効
	<%= f.radio_button :is_active, "Invalid" %> 退会済み

	<%= f.submit "変更を保存する", class:"btn btn-success" %>
<% end %>
</div>

